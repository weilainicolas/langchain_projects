# Lyrics Agent

Placeholder. Tuned in step 2.

Reads a SongBrief + a MusicSpec, emits Lyrics (see schema.py). Aligns line
count per section to MusicSpec.section_map, honors emotional_arc and
thematic_anchors from the brief, respects constraints.must_include /
must_avoid.
